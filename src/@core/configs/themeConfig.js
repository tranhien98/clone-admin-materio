const themeConfig = {
    // ** layout config
    templateName: "Tranhien Admin",
    mode: 'light',
    routingLoader: true,
    menuTextTruncate: true,
    navigatoinSize: 260,
    responsiveFontSize: true,
}

export default themeConfig;